module Test;

interface I { Int getF(); Int getP(); }

class C(Int p) implements I { 
    Int f = p - 1;
    Int getF() { return f; }
    Int getP() {return p; }
}

{
    I o = new C(100);
    Int p = o.getP();
    Int f = o.getF();

    assert p == 100;
    assert f == 99;
}