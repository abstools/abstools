This is a small example that demonstrates the use of the FFI (a.k.a. FLI).
It uses java.util.Random to generate a random number and prints it to standard output.

Note that at some point in the future "fli" will be replaced with "ffi".
