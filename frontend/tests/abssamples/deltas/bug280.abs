module M; export *;

class X {
	Unit m() { }
}

module N; export *;

interface I { Unit n(); }

module D;

import * from M;
import * from N;

delta K {
	modifies class X implements N.I {
		adds Unit n() { }
		modifies Unit m() { }
	}
}

productline PL {
	features X;
	delta K when X;
}

product P(X);


