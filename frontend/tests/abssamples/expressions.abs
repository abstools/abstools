{
z = true ; 
}
/*
def Int length(File file) =  
  case file {
    NilPacket => 0 ; 
    ConsPacket(p, list) => 1 + length(list) ;
  } 

def TPair revPair(TPair p) = let(T x) = fst(p) in pair(snd(p),x) 
def TPair revPair(TPair p) = let(T x) = 10 + 20  in pair(snd(p),x) 


	//MAIN
{  

//Strings 
y = "Hello world."; 



//IntExps with variables: 
x = length(list) ; 

x = 1 + x ; 

x = 1 + foo(x) ; 
x = 1 + foo( 1+2 ) ; 


x = 1 + fst(pair(a(1+2),b(3+four())));


//Integers 
x = 6 ;   
x = 6 * 7 + 8 ; 
x = 1 + 2 + 3 * 4 * 5  + 6 + 7 ;
x = 1 + 2 * 3 ; //  1 + (2*3)  
x = 1 * 2 + 3 ;  // (1*2) + 3 
x = 1 * (2 + 3); 
x = (1 + 2) * 3 ; 

 
//default operator precedence. 
x = (((1 + 2) + ((3 * 4) * 5))  + 6) + 7 ;

//Use of prantheses
x = (1 + 2) + ((((3 * 4) * 5) + 6) + 7) ;

//Mix
x = 1 + 2 / 3 * 4 - 7 % 6  ; 

skip; 

return 1 + 2 * 3   ;

return x ; 
}

*/