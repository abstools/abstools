//$Id$

data Bool = True | False; // builtin


class Foo {}
//Main
{

; 

b = b.append(i); 

//stmtReturn

//stmt
//assignStmt
if (a) skip ; else skip  ; 

if (a) skip  ; 
if (a) { skip ; skip ; }  ; 


a = b ; 
a = b.get ;
a = null ; 
a = True ;  
a = "foo" ;

a = new Foo() ; 
a = b!meth(); 
a = b!meth(c,d,e); 
a = b.meth(); 
a = b.meth(c,d,e); 
a = meth(c,d,e); 
a = meth(); 

//await g
await a? ; 
await b? & c? ; 
await b? & c & d; 

//skip
skip ; 

//If-then-else 
if (True) skip ; else skip ; 
//sblock
if (True) {skip ; skip ;} else skip  ; 

//optional else 
if (True) skip ; 

//nested
if (x)  
  if (y)  
    a = b ;
  else 
    skip ;
else 
  skip  ; 

//nested
if (x)  
  if (y)  
    a = b ; 

if (True) {a = b ; c = d ;} else skip ; 

suspend ; 

return null ;

}






