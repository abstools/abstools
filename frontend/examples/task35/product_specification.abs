product HighEndChat (VideoChat, VoiceChat, TextChat, FileTransfer) {
	RegularChat by Video2Voice pre Something post Something;
	LowEndChat by Video2Voice, Voice2Text pre Something post Something;
}

product RegularChat (VoiceChat, TextChat);

product LowEndChat (TextChat);

