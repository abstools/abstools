productline Chat_ProductLine;
 
features TextChat, VideoChat, VoiceChat, FileTransfer;

delta DVideo when VideoChat;
delta DVoice when VoiceChat ;
delta DFile when FileTransfer && VideoChat;

