module ABSChat.Main;

import * from ABSChat.Interfaces;  
import * from ABSChat.Server;
import * from ABSChat.Client;
import * from ABSChat.User;


{
    Server server; 
    server = new cog ServerImpl();  
	server ! start();
	
	Client client;  
	client = new cog ClientImpl();
	client ! setServer(server);
	client ! start();
	
	User user;
	user = new cog UserImpl();
	
	Fut<ClientGUI> fgui;
	fgui = client ! getGUI();
	[Somewhere] ClientGUI gui;
	gui = fgui.get;
	
	user ! use(gui);	
	
}