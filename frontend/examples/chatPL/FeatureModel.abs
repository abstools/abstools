root Chat {
  group allof {
    ChatMode {
      group [1..*] { Text, Voice, Video } 
    },
    opt FileTransfer
  }    	
}

extension Voice {
    require: Text;
}

extension Video {
    require: Voice;
}