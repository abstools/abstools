Issue the command

  mvn clean install

in this directory to locally build the documentation.  The generated HTML
files are in the target/classes/docs/html/ subdirectory.  Docbook XML is in
target/classes/docs/docbook/.

See http://asciidoctor.org/docs/asciidoc-syntax-quick-reference/ and
http://asciidoctor.org/docs/asciidoc-writers-guide/ for documentation on the
AsciiDoc syntax.
