Standard Data Types

  ABS has a number of standard data type, which are defined in the module
  <<<ABS.StdLib>>>:
  
  * <<<Unit>>>, which has only the single value <<<Unit>>>.

  * <<<Bool>>>, which has the two values <<<True>>> and <<<False>>>.

  * <<<Int>>>, which is an arbitrary integer <<<...,-2,-1,0,1,2,3,...>>>

  * <<<String>>>, which is a string that can be constructed by using double 
    quotes, e.g., <<<"this is a string">>>






