module NullableExample;

interface I extends J {
    [NonNull] J m([NonNull] I i);
}

interface J {
    
}

class D implements J {
    
}

class C implements I {
    [NonNull] J m([Nullable] I i) {
        J j = i;
        if (j == null) {
            j = new D();
        }
        return j;
    }
}
