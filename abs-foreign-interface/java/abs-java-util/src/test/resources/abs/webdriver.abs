module WebDriver;

export *;

import * from ABS.FLI;

interface WebDriver {
   Unit getPage(String s);
   String getCurrentUrl();
}

[Foreign] class FireFox implements WebDriver {
    Unit getPage(String s) { }   
    String getCurrentUrl() { return ""; } 
}