module M;
export *;
interface A {}
interface C extends B {}

features FF with true;

delta DD;
adds interface B extends A {}
/*adds class Q {
  Int i = "hi";
}*/
delta DD when FF;

module Main;
import * from M;
{
        A with {FF} a = null;
        B with {FF} b = null;
        C with {FF} c = null;
        b = a;
        c = a;
        c = b;
}
