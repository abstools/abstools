module M;
export *;
interface A {}
features F1,F2,F3,F4 with true;
delta D1;
modifies interface A{
    adds Unit m1();
}
delta D2;
modifies interface A{
    adds Unit m2();
}
delta D3;
modifies interface A{
    adds Unit m3();
}
delta D4;
modifies interface A{
    adds Unit m4();
}

delta D1 when F1;
delta D2 when F2;
delta D3 when F3;
delta D4 when F4;
module Main;
import * from M;
{
}