module M;
export *;
interface A {}
features F1,F2 with (!(!F1 || (F1 || F1)) || ((F1 || F1) || !F2)) || F1;
delta D1;
modifies interface A{
    adds Unit m();
}
delta D2;
modifies interface A{
    adds Unit m();
}

delta D1 when F1 && (!F1 && !F1);
delta D2 when !(((F2 && F2) && F1) || (!(F1 || F2) && ((F1 || F1) || (F1 && F2))));
module Main;
import * from M;
{}