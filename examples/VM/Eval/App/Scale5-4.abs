module M;
export *;
interface A {}
features F1,F2,F3,F4,F5 with ((!(F3 && F5) && (F4 && !F1)) && !!F3) || ((F4 && F1) && F3);
delta D1;
modifies interface A{
    adds Unit m();
}
delta D2;
modifies interface A{
    removes Unit m();
}
delta D3;
modifies interface A{
    removes Unit m();
}
delta D4;
modifies interface A{
    removes Unit m();
}
delta D5;
modifies interface A{
    adds Unit m();
}

delta D1 when !(F1 || (F1 && F1));
delta D2 when !!!!(F2 || F2);
delta D3 when !((F2 && !(F1 && F2)) || !!(F2 && F1));
delta D4 when ((!!F2 || !F4) || (F2 && !(F2 && F4))) && ((F3 || F1) && (F1 || F1));
delta D5 when !!(!F3 && (!F1 && !F1));
module Main;
import * from M;
{}